name: cross_final
description: "Final Flutter marketplace project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.9.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  chopper: ^8.4.0
  cloud_firestore: ^5.6.12
  cupertino_icons: ^1.0.8
  drift: ^2.29.0
  firebase_core: ^3.15.2
  flutter_riverpod: ^2.6.1
  go_router: ^17.0.0
  path: ^1.9.1
  path_provider: ^2.1.5
  shared_preferences: ^2.5.3
  sqlite3_flutter_libs: ^0.5.40

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.5.4
  drift_dev: ^2.31.0

flutter:
  uses-material-design: true
